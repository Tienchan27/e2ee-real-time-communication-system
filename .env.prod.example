BUILD_TARGET=runtime
NGINX_CONF=nginx.conf
POSTGRES_DB=e2ee_app
POSTGRES_USER=e2ee_user
POSTGRES_PASSWORD=change-me-on-server
# Per-service secrets: copy and edit api-service/.env, realtime-service/.env, frontend/.env
# from each .env.example — see docs/07-deployment-cicd.md secrets matrix
